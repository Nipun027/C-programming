#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int isPrime(int n){
    if(n <=1)
        return 0;
    if(n ==2)
        return 1;
    if(n % 2 == 0)
        return 0;
    for (int i = 3; i <= sqrt(n); i += 2){
        if(n % i == 0)
            return 0;
    }
       return 1;
}
int main() {
     int start, end;
    scanf("%d %d", &start, &end);
    int found = 0;
    for(int i = start; i <= end; i++){
        if (isPrime(i)){
            printf("%d ", i);
            found = 1;
        }
    }
     if (!found)
         printf("No prime numbers found");
        return 0;
}
