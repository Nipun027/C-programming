#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int N;
    scanf("%d", &N);
    if(N%N==0 || N%1==0)
    {
        printf("Prime");
        
    }
    else
    {
        printf("Not Prime");
    }
    return 0;
}
