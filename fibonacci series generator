#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int N;
    scanf("%d", &N);
    int a = 0, b = 1, next_term;
    
    for(int i=1; i<=N; ++i)
    {
        if(i == 1){
        printf("%d ", a);
            continue;
        }
        if(i == 2)
        {
            printf("%d ", b);
            continue;
        }
        next_term = a + b;
        printf("%d ", next_term);
        a = b;
        b = next_term;
        
    }
    
    return 0;
}
